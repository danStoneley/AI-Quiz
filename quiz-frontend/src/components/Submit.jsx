function Submit({ topic, handleSubmit }) {
    return (
                        <button
                    type="button"
                    className="btn btn-primary mt-3"
                    onClick={handleSubmit}
                >
                    submit
                </button>
    );
}

export default Submit;